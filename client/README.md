# client/

Laptop benchmark client and adapters.

## Purpose

This directory will contain the laptop-side measurement harness that:

- sends requests to local or reverse-tethered endpoints,
- records timestamps for TTFT and decode TPS computation,
- counts prompt and output tokens,
- writes structured logs.

## Status

Placeholder only. No code yet.

## Related docs

- `PROJECT_BRIEF.md` for scope
- `ARCHITECTURE.md` for ownership boundaries
- `EXPERIMENT_PROTOCOL.md` for metric definitions
